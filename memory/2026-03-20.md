
## Security Audit Results (04:00 AM)

Ran `security audit --deep` via scheduled cron. Key findings:

### High Priority
- **wecom plugin**: `potential-exfiltration` warnings in `src/monitor.ts:501` and `src/outbound.ts:208`
- **Plugin allowlist**: `plugins.allow` not set - any discovered plugin may load

### Medium Priority  
- `tools.fs.workspaceOnly=false` - tools can access arbitrary filesystem
- `agents.no_system_prompt` - no system prompt configured
- Several plugins use unpinned npm specs (supply chain risk)

### Low Priority / Good News
- Gateway secret exists ✓
- DB encryption enabled ✓
- Agent auth required ✓
- No anonymous access ✓
- MCP servers appear clean ✓
- No reverse shell / suspicious exec patterns ✓
- OpenClaw up to date ✓
- Trust model: personal assistant (single user) ✓
