# 2026-03-19 Security Audit Results

## Cron Job: `security audit --deep`

## Critical Issues (7)

### 1. Open groupPolicy with Elevated Tools
- **Risk:** Feishu group with `groupPolicy="open"` has elevated tools enabled
- **Impact:** Prompt injection in those rooms can become high-impact incident
- **Fix:** Set `groupPolicy="allowlist"` and keep elevated allowlists tight

### 2. Open groupPolicy with Runtime/Filesystem Tools
- **Risk:** Same open Feishu group exposes runtime + filesystem tools
- **Impact:** Prompt injection can trigger command/file actions
- **Fix:** Prefer `tools.profile="messaging"` for open groups, set `fs.workspaceOnly=true`

### 3-5. Plugin Code Safety - Multiple Plugins
- `.openclaw-install-stage-VyK5PO`: 4 critical (env-harvesting, dangerous-exec)
- `adp-openclaw`: 3 critical (env-harvesting, dangerous-exec)
- `qqbot`: 4 critical (env-harvesting, dangerous-exec)
- **Fix:** Review source code, remove if untrusted

### 6. Skill Code Safety - tavily
- **Location:** `/root/.openclaw/workspace/skills/tavily-search`
- **Issue:** 2x env-harvesting patterns in search scripts
- **Fix:** Review or remove skill

### 7. Feishu Security Warning
- **Issue:** `groupPolicy="open"` allows any member to trigger (mention-gated)
- **Fix:** Set `groupPolicy="allowlist"` + `groupAllowFrom` to restrict

## Warnings (7)
- Missing trusted proxies config
- Feishu doc create can grant permissions
- Multi-user setup detected (personal-assistant model warning)
- Extensions exist but no allowlist set
- Extension tools may be reachable under permissive policy
- Unpinned npm specs in plugins
- wecom: potential exfiltration patterns

## Info
- Groups: open=1, allowlist=0
- tools.elevated: enabled
- trust model: personal assistant

## Action Items
- [ ] Review and address critical plugin code safety issues
- [ ] Consider removing or hardening tavily skill
- [ ] Change Feishu groupPolicy from "open" to "allowlist"
- [ ] Set plugins.allow to explicit trusted plugin IDs
- [ ] Pin npm install specs for plugins
