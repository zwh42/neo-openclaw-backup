# 2026-03-13 Friday

## 04:00 - Security Audit (Scheduled)

Ran `openclaw security audit --deep`

### Critical Issues Found: 7

1. **security.exposure.open_groups_with_elevated** - feishu groupPolicy="open" + elevated tools
2. **security.exposure.open_groups_with_runtime_or_fs** - feishu + runtime/fs tools exposed
3. **fs.config.perms_world_readable** - /root/.openclaw/openclaw.json mode=644
4. **plugins.code_safety** - ".openclaw-install-stage-VyK5PO" dangerous code (env-harvesting, dangerous-exec)
5. **plugins.code_safety** - "adp-openclaw" dangerous code (env-harvesting, dangerous-exec)
6. **plugins.code_safety** - "qqbot" dangerous code (env-harvesting, dangerous-exec)
7. **skills.code_safety** - "tavily" skill dangerous code (env-harvesting)

### Warnings: 7
- gateway.trustedProxies missing
- feishu doc create permissions
- Multi-user setup heuristic warning
- Extensions without allowlist
- Plugin tools reachable
- NPM specs unpinned
- "wecom" suspicious patterns

### Recommended Fixes
- chmod 600 /root/.openclaw/openclaw.json
- Set feishu groupPolicy="allowlist"
- Review/remove suspicious plugins
- Set plugins.allow explicit list
