# 2026-03-14 Daily Log

## 04:00 - Scheduled Security Audit (cron)

Ran `openclaw security audit --deep`

### Critical Findings:
1. **Open groupPolicy + Elevated tools** - feishu channel has `groupPolicy="open"` with tools.elevated enabled
2. **Open groupPolicy + Runtime/FS tools** - Same feishu config exposes exec, process, read, write, edit tools
3. **Plugin code safety issues** - 4 plugins with dangerous patterns:
   - .openclaw-install-stage-VyK5PO (env-harvesting, dangerous-exec)
   - adp-openclaw (env-harvesting, dangerous-exec)  
   - qqbot (env-harvesting, dangerous-exec)
   - tavily skill (env-harvesting x2)

### Warnings:
- Missing trustedProxies config
- Extensions without allowlist
- Unpinned npm specs (qqbot, ddingtalk, wecom, adp-openclaw)
- Potential multi-user setup detected

### Recommended Fixes (not applied):
- Set feishu groupPolicy="allowlist"
- Set agents.defaults.sandbox.mode="all"
- Set tools.fs.workspaceOnly=true
- Review/remove suspicious plugins
- Pin npm install specs

---
