# 2026-03-18 Daily Log

## 04:00 - Security Audit (Scheduled Cron)

Ran `openclaw security audit --deep` - **7 critical issues found**:

### Critical Issues
1. **Open groupPolicy + elevated tools** - Feishu channel has `groupPolicy="open"` with tools.elevated enabled
2. **Open groupPolicy + runtime/fs** - Same feishu config exposes exec/process/read/write/edit tools
3. **Plugin dangerous code**: `.openclaw-install-stage-VyK5PO` - env-harvesting + dangerous-exec patterns
4. **Plugin dangerous code**: `adp-openclaw` - env-harvesting + dangerous-exec patterns  
5. **Plugin dangerous code**: `qqbot` - env-harvesting + dangerous-exec patterns
6. **Skill dangerous code**: `tavily-search` - env-harvesting patterns
7. **Feishu open group warning** - Any member can trigger mentions

### Warnings
- `gateway.trustedProxies` not configured
- Extensions loaded without `plugins.allow` allowlist
- NPM specs unpinned for qqbot, ddingtalk, wecom, adp-openclaw
- Potential multi-user setup detected

### Recommendations (from audit)
- Set `groupPolicy="allowlist"` for feishu
- Enable `plugins.allow` with explicit trusted plugin IDs
- Pin NPM versions for plugins
- Consider `tools.profile="messaging"` for open groups
- Set `tools.fs.workspaceOnly=true`

---
