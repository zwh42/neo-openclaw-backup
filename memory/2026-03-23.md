# 2026-03-23 Daily Memory

## Security Audit (04:00 AM)

Ran `openclaw security audit --deep`. Results compared to 2026-03-22:

**Same as yesterday (unresolved):**
- Attack surface: Feishu open group + elevated tools exposed
- Extensions exist but `plugins.allow` not set (6 extensions: `.openclaw-install-stage-vyk5po`, `adp-openclaw`, `ddingtalk`, `qqbot`, `skillhub`, `wecom`)
- Permissive tool policy contexts (default)
- Unpinned npm specs: `qqbot`, `ddingtalk`, `wecom`, `adp-openclaw`
- Plugin `.openclaw-install-stage-vyk5po` — env-harvesting + dangerous exec patterns

**NEW in today's scan:**
- Plugin `wecom` — 2x `[potential-exfiltration]` warnings (file read + network send in `src/monitor.ts:501` and `src/outbound.ts:208`)

**Not flagged today:**
- Skill `tavily-search` — not flagged (possibly cleaned up or differently flagged)

**Overall:** No improvement. 飞书 open group + 插件供应链风险 依然存在。wecom 新增 exfiltration 可疑警告。
