# 2026-03-22 Daily Log

## OPC Daily Briefing
- 生成并保存 OPC 简报至 /root/.openclaw/workspace/reports/opc-briefing-2026-03-22.md
- 安全问题与昨日相同（7项 critical 持续未处理）

## Security Audit (Scheduled 04:00)

Ran `openclaw security audit --deep`. Results:

### Critical Issues (7)
1. **Feishu open groupPolicy + elevated tools** — `channels.feishu.groupPolicy="open"` with `tools.elevated` enabled. High risk if prompt injection occurs.
2. **Feishu open groupPolicy + runtime/fs exposed** — Same open group with exec/write/edit tools accessible.
3. **Plugin .openclaw-install-stage-VyK5PO** — 4 critical issues: env-harvesting + dangerous exec patterns. Needs review.
4. **Plugin adp-openclaw** — 3 critical issues: env-harvesting + dangerous exec patterns.
5. **Plugin qqbot** — 4 critical issues: env-harvesting + dangerous exec patterns.
6. **Skill tavily-search** — 2 critical env-harvesting issues in scripts.
7. **Feishu doc_owner_open_id** — Doc create can grant permissions to requesting user.

### Warnings (7)
1. **gateway.trusted_proxies_missing** — Reverse proxy headers not trusted (bind is loopback anyway).
2. **channels.feishu.groupPolicy open** — Allowlist mode recommended.
3. **Multi-user heuristic detected** — Personal assistant model vs multi-tenant exposure risk.
4. **plugins.extensions_no_allowlist** — 6 extensions loaded but no `plugins.allow` set.
5. **plugins.tools_reachable_permissive_policy** — Extension tools reachable under permissive policy.
6. **plugins.installs_unpinned_npm_specs** — qqbot, ddingtalk, wecom, adp-openclaw all unpinned.
7. **Plugin wecom** — 2 potential exfiltration warnings (file read + network send).

### Info
- Attack surface: 1 open group, elevated tools enabled, browser control enabled
- Trust model: personal assistant (single trusted operator)

### Recommended Fixes
- Set `channels.feishu.groupPolicy="allowlist"` + `groupAllowFrom`
- Set `plugins.allow` to explicit trusted plugin ids
- Pin npm install specs to exact versions
- For open groups: use `tools.profile="messaging"` or deny group:runtime/group:fs
- Set `agents.defaults.sandbox.mode="all"` and `tools.fs.workspaceOnly=true`
- Review/remove tavily-search skill if untrusted

